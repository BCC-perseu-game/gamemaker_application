/// @description Inserir descrição aqui
// Você pode escrever seu código neste editor

// Inherit the parent event
event_inherited();

//desenhando linha para ver se a distancia enta ok
//draw_line(x, y - sprite_height/4, x + (dist * xscale) + 60, y - sprite_height/4);

 // Coordenadas da linha de visão
    //var start_x = x;
    //var start_y = y - sprite_height / 4;
    //var end_x = start_x + (-(dist+15)* xscale);
    //var end_y = start_y;

    // Definir cor e desenhar linha
   // draw_set_color(c_red);
   // draw_line(start_x, start_y, end_x, end_y);
    //draw_set_color(c_white); // Resetar a cor